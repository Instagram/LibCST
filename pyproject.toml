[tool.black]
target-version = ["py37"]
extend-exclude = "native/.*"

[tool.ufmt]
excludes = ["native/", "stubs/"]

[tool.slotscheck]
exclude-modules = '^libcst\.(testing|tests)'

[build-system]
requires = ["setuptools", "wheel", "setuptools-rust"]
